pydantic~=2.10.6
openai~=1.66.3
tenacity~=9.0.0
pyyaml~=6.0.2
loguru~=0.7.3
structlog>=24.1.0,<26.0.0
# numpy нужен pandas и matplotlib. Тяжёлый ML-груз (browsergym, gymnasium,
# datasets, huggingface-hub) убран: он тянул torch и весь набор NVIDIA CUDA
# на несколько гигабайт, а в коде не использовался ничем.
numpy
fastapi~=0.115.11
tiktoken~=0.9.0

html2text~=2024.2.26
pillow>=10.4,<11
uvicorn~=0.34.0
unidiff~=0.7.5
uv>=0.6.0
googlesearch-python~=1.3.0
baidusearch~=1.0.3
duckduckgo_search~=7.5.3

aiofiles~=24.1.0
pydantic_core~=2.27.2
colorama~=0.4.6
playwright~=1.51.0

docker~=7.1.0
pytest~=8.3.5
pytest-asyncio~=0.25.3

mcp~=1.5.0
httpx>=0.27.0
tomli>=2.0.0

boto3~=1.37.18

requests~=2.32.3
beautifulsoup4~=4.13.3

# разбор документов, которые скачивает инструмент fetch, и таблиц, с которыми
# работает агент: раньше они держались на транзитивных зависимостях
pypdf~=5.1
openpyxl~=3.1

# счёт и графики, которые агент строит через python_execute. pandas до сих пор
# держался на транзитивных зависимостях, а matplotlib не было вовсе: агент
# получал ModuleNotFoundError вместо картинки. Нижние границы, а не точные
# версии, — чтобы не ломать то, что уже стоит в образе.
pandas>=2.2
matplotlib>=3.8

setuptools~=75.8.0
